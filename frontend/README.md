# Fleet Management BI — Frontend

React + Vite SPA for the Fleet Management BI dashboard.

Setup, environment variables, and full project documentation are in the [root README](../README.md).

```bash
npm install
cp .env.example .env
npm run dev
```
